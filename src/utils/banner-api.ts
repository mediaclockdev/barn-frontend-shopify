import { fetchWcApi } from "./api-client";
import { buildUrl, ENDPOINTS } from "./api-endpoints";

/**
 * Service to fetch dynamic content for the Top Banner.
 */
export async function getBannerData(): Promise<{ data: any } | null> {
  return null;
}
