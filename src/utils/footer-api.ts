import { fetchWcApi } from "./api-client";
import { buildUrl, ENDPOINTS } from "./api-endpoints";

/**
 * Service to fetch dynamic content for the Footer.
 */
export async function getFooterData(): Promise<{ data: any } | null> {
  return null;
}
